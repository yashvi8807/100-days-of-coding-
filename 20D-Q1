// Q39: Write a program to find the product of odd digits of a number.
#include <stdio.h>
int main() {
    int n, rem;
    long product = 1;  
    printf("Enter number.");
    scanf("%d", &n);
    while (n != 0) {
        rem = n % 10;          
        if (rem % 2 != 0) {    
            product *= rem;   
            hasOdd = 1;        
        }
        n = n / 10;            
    }
    printf("%ld\n", product);
    return 0;
}
