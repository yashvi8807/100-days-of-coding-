//Q67: Insert an element in an array at a given position.
#include<stdio.h>
int main()
{
    int n;
    printf("Enter the size of element.");
    scanf("%d",&n);
    int arr[n+1];
    printf("Enter the elements of array.");
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    int pos,x;
    printf("Enter the postion and the number which you want to insert.");
    scanf("%d %d",&pos,&x);
    for(int i=n;i>=pos;i--){
        arr[i]=arr[i-1];
    }
    arr[pos]=x;
    for(int i=0;i<=n;i++){
        printf("%d ",arr[i]);
    }
    return 0;
}
